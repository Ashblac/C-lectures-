#include <iostream>
using namespace std;

int main() {
    cout << "=======================================" << endl;
    cout << "=== POINTERS AND REFERENCES LAB ===" << endl;
    cout << "=======================================" << endl;

    // SECTION 1: Basic Pointer Operations
    cout << "\n1. BASIC POINTER OPERATIONS" << endl;
    cout << "---------------------------" << endl;

    // 1.1) TODO: Define variables and pointers
    int a = 5;
    int b = 10;
    int* aPtr = &a;
    int* bPtr = &b;

    cout << "Variables initialized:" << endl;
    cout << " a = " << a << ", b = " << b << endl;
    cout << " aPtr = " << aPtr << ", bPtr = " << bPtr << endl << endl;

    // 1.2) Demonstrate different ways to access a
    cout << "Accessing variable 'a':" << endl;
    cout << " Address using & operator: " << &a << endl;
    cout << " Address using aPtr:       " << aPtr << endl;
    cout << " Value using variable:     " << a << endl;
    cout << " Value using * operator:   " << *aPtr << endl << endl;

    // 1.3) TODO: Repeat step 2 for b
    cout << "Accessing variable 'b':" << endl;
    cout << " Address using & operator: " << &b << endl;
    cout << " Address using bPtr:       " << bPtr << endl;
    cout << " Value using variable:     " << b << endl;
    cout << " Value using * operator:   " << *bPtr << endl << endl;

    // 1.4) Memory layout discussion (Skipped code for brevity, same as skeleton)

    // 1.5) TODO: Declare an integer 'q' with value 15 and a pointer 'qPtr' to it.
    int q = 15;
    int* qPtr = &q;
    cout << "Variable q = " << q << " at address " << qPtr << endl << endl;

    // SECTION 2: Pointer Arithmetic and Operations
    cout << "\n2. POINTER OPERATIONS" << endl;
    cout << "---------------------" << endl;

    // 2.1 & 2.2 (Skipped code for brevity, same as skeleton)

    // 2.3) Expanded pointer arithmetic demo
    cout << "Pointer Arithmetic:" << endl;
    int arr[] = {10, 20, 30}; // TODO: Initialize array
    int* arrPtr = arr;        // TODO: Initialize pointer
    
    cout << " First 3 elements using indexing: " << arr[0] << " " << arr[1] << " " << arr[2] << endl;
    cout << " arrPtr points to: " << *arrPtr << " (address: " << arrPtr << ")" << endl;

    arrPtr++; // TODO: increment
    cout << " After arrPtr++: " << *arrPtr << " (address: " << arrPtr << ")" << endl;
    
    arrPtr--; // TODO: decrement
    cout << " After arrPtr--: " << *arrPtr << " (address: " << arrPtr << ")" << endl;

    cout << " Access arr[1] using offset: " << *(arrPtr + 1) << endl;
    cout << " Incrementation size: " << sizeof(int) << " bytes" << endl;

    // TODO: Use arrPtr to access arr[2] using pointer arithmetic
    cout << " Accessing arr[2] via arithmetic: " << *(arrPtr + 2) << endl;

    // SECTION 3: Pointer Manipulation
    cout << "\n3. POINTER MANIPULATION" << endl;
    cout << "-----------------------" << endl;
    
    aPtr = bPtr; // aPtr now points to b
    cout << "\nAfter aPtr = bPtr:" << endl;
    
    // 3.2) Modify through pointer
    *bPtr = 9; // TODO: Change value bPtr points to 9
    cout << "\nAfter modifying using *bPtr:" << endl;
    cout << " b = " << b << " (changed through pointer)" << endl;
    cout << " *aPtr = " << *aPtr << " (also changed - same location)" << endl;

    // SECTION 4: References
    // 4.5) TODO: Exercise
    int& d = b; // TODO: Create reference d
    d = 12;     // TODO: Assign 12
    cout << "Ref d assigned 12. b = " << b << ", Address of b = " << &b << endl;

    // ... (Remaining summary sections as per skeleton)
    return 0;
}
